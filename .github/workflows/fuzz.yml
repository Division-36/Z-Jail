name: fuzz
on: [workflow_dispatch]
jobs:
  fuzz:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: sudo apt-get install -y clang
    - run: make -C fuzz && ./fuzz/fuzz_bpf -max_total_time=30
